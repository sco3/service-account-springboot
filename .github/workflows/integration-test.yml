name: Integration Test

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  integration-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Java 25
        uses: actions/setup-java@v4
        with:
          java-version: '25'
          distribution: 'temurin'

      - name: Start Keycloak and ClickHouse
        run: docker compose -f docker-compose.infra.yml up -d 

      - name: Wait for ClickHouse
        run: |
          echo "Waiting for ClickHouse..."
          for i in $(seq 1 60); do
            if curl -sf http://localhost:8123/ping > /dev/null 2>&1; then
              echo "ClickHouse ready after ${i}s"
              exit 0
            fi
            sleep 1
          done
          echo "ClickHouse failed to start"
          docker compose logs clickhouse
          exit 1

      - name: Wait for Keycloak
        run: |
          echo "Waiting for Keycloak..."
          for i in $(seq 1 60); do
            if curl -sf http://localhost:8081/realms/service-accounts/.well-known/openid-configuration > /dev/null 2>&1; then
              echo "Keycloak ready after ${i}s"
              exit 0
            fi
            sleep 1
          done
          echo "Keycloak failed to start"
          docker compose logs keycloak
          exit 1

      - name: Check all service ports
        run: |
          echo "Checking service ports 8123, 8081 ..."
          netstat -ltnp | grep -E '8123|8081'

      - name: Start Spring Boot app
        run: |
          nohup ./gradlew bootRun > app.log 2>&1 &
          echo "Waiting for Spring Boot app..."
          for i in $(seq 1 120); do
            if curl -sf http://localhost:8080/actuator/health > /dev/null 2>&1; then
              echo "App ready after ${i}s"
              exit 0
            fi
            sleep 1
          done
          echo "App failed to start"
          cat app.log
          exit 1

      - name: Integration Test
        run: |
          ./gradlew ci --stacktrace

      - name: Cleanup
        if: always()
        run: docker compose down -v
